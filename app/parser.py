import requests, re
from bs4 import BeautifulSoup




# images = soup.findAll('img', {'class': 'progressiveMedia-noscript js-progressiveMedia-inner'})
# for image in images:
#     print(image['src'])

# img = soup.find_all('img', attrs={'src': re.compile("^http://")})
# imgs=[]
#
# for img in soup.findAll('img', attrs={'src': re.compile("^http://")}):
#     imgs.append(img.get('src'))
#
# print(imgs)